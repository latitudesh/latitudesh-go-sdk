# CreateElasticIPMode

How the elastic IP is delivered. Defaults to routed

## Example Usage

```go
import (
	"github.com/latitudesh/latitudesh-go-sdk/models/components"
)

value := components.CreateElasticIPModeRouted
```


## Values

| Name                        | Value                       |
| --------------------------- | --------------------------- |
| `CreateElasticIPModeRouted` | routed                      |
| `CreateElasticIPModeBgp`    | bgp                         |